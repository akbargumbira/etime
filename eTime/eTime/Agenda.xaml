<UserControl x:Class="eTime.Agenda"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="Auto" d:DesignWidth="480">
    
    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
        <StackPanel>
            <TextBlock Name="textBlockTitle"    Text="" />
            <TextBlock Name="textBlockDesc"     Text="" />
            <TextBlock Name="textBlockStart"    Text="" />
            <TextBlock Name="textBlockEnd"      Text="" />
            <TextBlock Name="textBlockLocation" Text="" />
        </StackPanel>
    </Grid>
</UserControl>
